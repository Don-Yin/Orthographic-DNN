from utils.data_generate.main import (init_create_prime_data,
                                      init_create_train_data)

if __name__ == "__main__":
    init_create_train_data(dummy=False, random=True)
    # init_create_prime_data(position_correction=True)
    # init_create_prime_data(position_correction=True)
